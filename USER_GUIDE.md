# Quackbase 用户使用说明

> 一个浏览器里就能用的数据探索平台。把 CSV / XLSX / ZIP 拖进来，就能像 Excel 一样浏览、筛选、搜索、导出，但比 Excel 快得多，也能装得下千万级行数据。

---

## 目录

- [一、能用它做什么](#一能用它做什么)
- [二、登录](#二登录)
- [三、上传数据](#三上传数据)
  - [3.1 单个 CSV / XLSX 文件](#31-单个-csv--xlsx-文件)
  - [3.2 批量上传多个文件](#32-批量上传多个文件)
  - [3.3 ZIP 压缩包上传（推荐用于多文件 / 大文件）](#33-zip-压缩包上传推荐用于多文件--大文件)
  - [3.4 多 Sheet 的 XLSX](#34-多-sheet-的-xlsx)
  - [3.5 上传模式：新建表 vs 追加到已有表](#35-上传模式新建表-vs-追加到已有表)
- [四、浏览与查询数据](#四浏览与查询数据)
  - [4.1 切换数据表](#41-切换数据表)
  - [4.2 排序](#42-排序)
  - [4.3 筛选（按列条件）](#43-筛选按列条件)
  - [4.4 全局搜索](#44-全局搜索)
  - [4.5 分页](#45-分页)
- [五、导出数据](#五导出数据)
- [六、删除数据](#六删除数据)
- [七、用户与权限](#七用户与权限)
- [八、常见问题](#八常见问题)

---

## 一、能用它做什么

| 场景 | 怎么做 |
|---|---|
| 把一份大 CSV 丢进来快速看一眼 | 拖文件 → 选表 → 看表格 |
| 把一个项目下几十个表批量入库 | 把它们打成 ZIP → 一键上传 |
| 一周追加一次新数据到同一张表 | 选"追加到已有表"模式上传 |
| 在千万级数据中搜一段文字 | 用顶部搜索框 |
| 按时间范围 / 数值范围 / 文本筛选数据 | 用筛选条 |
| 把筛选后的结果导出给别人 | 点导出按钮，自动下载 CSV |

支持的文件格式：

- `.csv` —— 自动识别 UTF-8 / GBK / GB18030 等编码，无需手动转码
- `.xlsx` —— 自动转成 CSV 入库；多 Sheet 表头一致时自动合并
- `.zip` —— 解压后递归找 csv/xlsx 批量入库

---

## 二、登录

浏览器打开访问地址（部署同事会告诉你），输入用户名 / 密码即可。

首次使用的默认账号：

| 用户名 | 密码 |
|---|---|
| `admin` | `admin123` |

> 登录后请立即点头像 → 修改密码。

---

## 三、上传数据

左侧上传区有两种方式：

- **点击** 上传区 → 弹出系统文件选择框
- **拖拽** 一个或多个文件到上传区

### 3.1 单个 CSV / XLSX 文件

1. 拖入 / 选中一个 `.csv` 或 `.xlsx`
2. 弹出"选择导入方式"窗口
3. 选 **新建表**（默认），可改表名（默认用文件名）
4. 点 **确认导入** → 表立刻出现在左侧表列表

### 3.2 批量上传多个文件

一次拖入多个 csv/xlsx，弹窗会列出所有文件，每个文件可单独：
- 修改表名
- 点 ✕ 移除不想导入的

支持勾选"忽略以 `#` 开头的注释行"——CSV 文件头部有说明文字时勾上即可。

### 3.3 ZIP 压缩包上传（推荐用于多文件 / 大文件）

**适用场景**

- 一次要上传几十个表 —— 打成 zip 比一个个拖快得多
- 单个 CSV 太大（≥ 500MB）—— 系统会**强制要求**先压缩成 zip 再上传

**操作步骤**

1. 把若干 csv / xlsx（可以放在子目录里）打包成 `xxx.zip`
2. 拖入上传区
3. 系统自动解压并展示清单：
   ```
   📦 来自 ZIP：报表.zip（5 个文件）
   ┌────────────────────────────────────────┐
   │ 📄 销售/华东.csv          表名: [销售_华东] │
   │ 📄 销售/华南.csv          表名: [销售_华南] │
   │ 📄 库存/2026Q1.xlsx       表名: [库存_2026Q1]│
   │ ...                                       │
   └────────────────────────────────────────┘
   ```
4. 每行可单独修改表名（默认用相对路径自动生成，避免同名冲突）
5. 点 **确认导入** → 所有文件依次入库，每张表入库结果以 toast 提示

**ZIP 上传的规则**

| 情况 | 行为 |
|---|---|
| ZIP 内含 csv / xlsx 以外的文件（如 `.txt`、图片） | **整个 ZIP 拒绝**，需清理后重传 |
| ZIP 内有子目录 | 递归扫描，全部导入 |
| 不同子目录有同名文件（如 `dir1/a.csv` 和 `dir2/a.csv`） | 自动用相对路径区分表名（`dir1_a` vs `dir2_a`） |
| ZIP 内的 macOS 元数据（`__MACOSX/`、`.DS_Store`） | 自动跳过，不会报错 |
| ZIP 中某个文件解析失败（列结构有问题等） | 该文件失败，**其他文件继续导入**，弹窗各自提示 |
| 同时拖入 ZIP 和 csv 混合 | 拒绝，提示"一次只能上传一个 ZIP，且不能与其他文件混合" |
| ZIP 模式 | 强制 **新建表（replace）**；不支持追加模式 |

**为什么 CSV ≥ 500MB 必须压缩？**

- 上传更快（CSV 文本压缩率通常 70%+）
- 服务器接收上限是 500MB；超过会被网关拒绝
- 系统会前置拦截：选择文件时如果检测到 ≥ 500MB 的 CSV，会直接弹错"请先压缩为 ZIP 再上传"

### 3.4 多 Sheet 的 XLSX

上传单个 xlsx 时，如果文件里有多个 Sheet，会进入 **多 Sheet 导入预览**：

- 系统按 **表头是否一致** 自动分组（同表头多个 Sheet 会被建议合并成一张表）
- 每个分组可：
  - 勾选是否导入
  - 修改表名
  - 看到包含哪些 Sheet 和列名
- 点 **确认导入这些分组** 完成

### 3.5 上传模式：新建表 vs 追加到已有表

普通 csv/xlsx 上传时（非 zip），有两个互斥的模式：

| 模式 | 含义 |
|---|---|
| **新建表**（默认） | 每个文件创建独立的新表；如果同名表存在会被**整体替换** |
| **追加到已有表** | 把数据**追加**到指定的已有表中（要求两边列名完全一致） |

追加模式下，文件的列名必须和目标表一模一样，否则会报"列结构不匹配"，**不会**部分追加。

---

## 四、浏览与查询数据

### 4.1 切换数据表

左侧表列表显示所有已上传的表，每行有：
- 表名
- 行数
- 列数

点表名即可切到该表。

### 4.2 排序

点列头一次 → 升序，再点 → 降序，再点 → 取消。时间戳 / 数值 / 文本都按各自类型排序。

### 4.3 筛选（按列条件）

点顶部 **筛选** → 添加条件：

- 选列名
- 选操作符：`=` / `!=` / `>` / `<` / `>=` / `<=` / `LIKE` / `IS NULL` / `IS NOT NULL`
- 输入值

多个条件之间是 **AND**（同时满足）。

**时间戳列示例**：

```
列：支付完成时间
操作：>=
值：2026-04-15 23:59:59
```

系统会自动把字符串值转成 TIMESTAMP 与列做比较，无需手动写 SQL。

### 4.4 全局搜索

顶部搜索框输入关键词，会在 **所有列** 里做不区分大小写的模糊匹配（包括数值列也会被转成文本搜索）。

可以与筛选条件叠加 —— 先按时间筛选，再搜文字，结果是两者的交集。

### 4.5 分页

底部分页栏可：
- 切到任意页
- 修改每页条数（默认 50）
- 看到总行数 / 总页数

---

## 五、导出数据

点 **导出 CSV** 按钮 → 浏览器自动下载。

| 选项 | 行为 |
|---|---|
| 当前已应用筛选 / 搜索 | 默认导出**筛选后**的数据 |
| 想导出全表 | 勾"导出全部"（忽略当前筛选） |

导出文件特性：
- 自带 UTF-8 BOM，Excel 打开不会乱码
- 流式下载，几百万行也不会卡浏览器
- 文件名自动带表名

---

## 六、删除数据

| 操作 | 入口 | 影响 |
|---|---|---|
| 删除整张表 | 表列表 → 表名右侧的"⋯" → 删除 | 该表及其数据全部消失，**不可恢复** |
| 按条件删除部分行 | 应用筛选条件后 → "删除筛选结果"按钮 | 只删除匹配的行，其他行保留 |
| 清空全部上传过的源文件 | 文件管理 → 清空 | 仅删 `uploads/` 下的源文件，**不影响已入库的表** |

> 删除是物理删除，没有回收站。重要数据请先导出备份。

---

## 七、用户与权限

| 角色 | 上传 | 查看 | 删除 | 用户管理 |
|---|---|---|---|---|
| **admin** | ✅ | ✅ | ✅ | ✅ |
| **editor** | ✅ | ✅ | ✅ | ❌ |
| **viewer** | ❌ | ✅ | ❌ | ❌ |

每个用户的数据是 **完全隔离** 的 —— 你只能看到你自己上传的表，admin 也不会越权访问别人的库。

**用户管理（仅 admin）**：
- 顶部菜单 → 用户管理
- 可以新增用户、改密码、改角色、删除用户

---

## 八、常见问题

**Q1. 上传 CSV 中文乱码？**

系统会自动识别 GBK / GB18030 / UTF-8 等编码并转成 UTF-8 入库，正常情况不会乱码。如果出现问题，请确认原文件的实际编码——某些工具导出的"假 UTF-8"（实际是 ANSI）可能识别失败。

**Q2. 上传 xlsx 后只看到部分数据？**

xlsx 多 Sheet 时，系统只合并 **表头完全一致** 的 Sheet。表头不同的 Sheet 会被忽略。如果你想全部入库，请用多 Sheet 预览功能逐个建表。

**Q3. CSV 文件第一行不是表头怎么办？**

如果是 `#` 开头的注释行，勾选上传弹窗里的 **忽略注释行**。如果就是数据行混入了说明文字，请先在 Excel 里清理后再上传。

**Q4. 追加上传时报"列结构不匹配"？**

追加模式要求源文件和目标表的列名 **完全一致**（顺序可不同，但名字得一字不差）。即使多了一列或少了一列都会拒绝。建议要么改源文件、要么用"新建表"覆盖。

**Q5. 上传 ZIP 报"包含不支持的文件"？**

ZIP 里只能放 csv / xlsx。常见误入：
- README.txt / .md → 删掉
- 备份文件（如 `~$xxx.xlsx`，xlsx 编辑时的临时文件）→ 删掉
- 图片、PDF → 删掉

清理后重新打包再上传即可。

**Q6. 单 CSV 大小被拦截怎么办？**

这是设计行为：≥ 500MB 的 CSV 必须压缩成 ZIP 再上传。

```
原 CSV 1.2GB → 压成 zip 通常 200-400MB
```

打包方法：
- macOS：右键 → 压缩
- Windows：右键 → 发送到 → 压缩文件
- 命令行：`zip xxx.zip xxx.csv`

**Q7. 时间筛选报"TIMESTAMP and VARCHAR"错误？**

系统已自动处理时间列的类型转换。如果仍遇到，请确认填入的值格式是否正确：

| 列类型 | 推荐格式 |
|---|---|
| TIMESTAMP | `2026-04-15 23:59:59` 或 `2026-04-15T23:59:59` |
| DATE | `2026-04-15` |

**Q8. 数据丢失 / 误删了怎么办？**

系统没有回收站。重要数据建议：
1. 上传前留好原始文件
2. 重要表定期 **导出 CSV** 做备份
3. 删除前再三确认

---

## 反馈与建议

使用过程中遇到 bug 或有功能建议，请联系系统管理员。

> 🦆 Happy Quacking!
